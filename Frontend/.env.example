# Local development. On Vercel replace with the backend project's HTTPS origin.
BACKEND_API_URL="http://127.0.0.1:4000"
# If the backend has Vercel Deployment Protection, use its automation bypass secret.
# Set this only on the frontend server; never use a NEXT_PUBLIC_ prefix.
BACKEND_VERCEL_BYPASS_SECRET=""
