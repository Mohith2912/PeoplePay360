import { z } from "zod";
export const employeeCreateSchema = z.object({ employeeCode: z.string().min(1).max(30), firstName: z.string().min(1).max(80), lastName: z.string().min(1).max(80), email: z.string().email(), phone: z.string().optional(), dateOfJoining: z.coerce.date(), department: z.string().min(1), designation: z.string().min(1), employeeType: z.string().optional(), managerId: z.string().optional(), workingScheduleId: z.string().optional(), status: z.enum(["ACTIVE", "NOTICE_PERIOD", "TERMINATED"]).default("ACTIVE"), panNumber: z.string().optional(), bankName: z.string().optional(), bankAccountNumber: z.string().optional(), ifscCode: z.string().optional(), uanNumber: z.string().optional(), pfNumber: z.string().optional(), esiNumber: z.string().optional(), location: z.string().optional() });
